from functions import square (will trigger the same as below)
# import functions

for i in range(10):
    print(f"The square of {i} is {square(i)}")
# print(f"The square of {i} is {functions.square(i)}") (it works just the same as above)